
server:
  port: 0
cloud:
  aws:
    region:
      static: ap-northeast-2
    s3:
      bucket: devday-bucket
    credentials:
      access-key: AKIAXEUXRPXD3GDITUSE
      secret-key: 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
    stack:
      auto: false
spring:
  application:
    name: challenge-service
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:devday
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    generate-ddl: true

eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka

logging:
  level:
    com.example.challengeservice.client: DEBUG


gateway:
  ip: 192.168.100.144
